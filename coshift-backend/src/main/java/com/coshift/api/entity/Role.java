package com.coshift.api.entity;

public enum Role {
    USER,        // Utilisateur standard (Passager/Conducteur)
    ADMIN,       // Administrateur d'une organisation (Client B2B)
    SUPER_ADMIN  // Moi (Gestionnaire de toute la plateforme CoShift)
}